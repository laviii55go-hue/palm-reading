# 作業履歴 2025-03-24

## 楽天ウィジェット（`app/components/RakutenWidget.tsx`）

- `rakuten_widget.js` を `https://` 固定＋`?20230106` で読み込み。同一ページでの二重 `<script>` 追加を防止。
- A8 ネットワーク連携用のインライン変数（`rakuten_adNetworkId` / `rakuten_adNetworkUrl` / `rakuten_pointbackId` / `rakuten_mediaId`）を削除（`mtwidget05.affiliate…` 経由の不正応答による SyntaxError 回避のため）。
- `rakuten_ts` を `Date.now()` で付与。A8 計測用 1×1 画像は維持。

## タロット API（`app/api/tarot-result/route.ts`）

- AI プロバイダを **ランダム1回**から、**OpenAI → Anthropic → Gemini** の優先順で順次試行するよう変更（1プロバイダの一時失敗で即テンプレになるのを緩和）。
- `parseTarotAiJson`: Claude が返す ` ```json ` フェンス付き応答をパース可能に。
- 各 API キーは `.trim()` ありで空文字は未設定扱い。

## タロット3択ページ（`app/tarot/page.tsx`）

- 見出し「タロット3択占い」から **🎴 絵文字のみ削除**（花札に見えるための要望）。`/tarot-top.png`・`/tarot-back.png` は表示維持。

## Git

- `main` にプッシュ済み（コミット: タロット AI フォールバック強化＋楽天ウィジェット調整）。※本記録作成時点の状態。
