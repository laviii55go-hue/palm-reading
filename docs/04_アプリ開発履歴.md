# アプリ開発履歴（抜粋・追記用）

このファイルは機能単位の経緯を短く残す。日ごとの詳細は `YYYY-MM-DD_作業履歴.md` を参照。

---

## 2025-03-24

- **タロット（`/tarot`）**: `POST /api/tarot-result` で AI プロバイダを複数順に試行。Claude 応答の JSON フェンス対応。本番の「テンプレート固定」になりやすい問題の緩和。
- **楽天ウィジェット**: `RakutenWidget` で `rakuten_widget.js` の読み方整理、A8 ネットワーク用インライン変数を外して `mtwidget05` 系の SyntaxError を抑止。
- **タロット UI**: 見出しの 🎴 絵文字のみ削除（画像アセットは維持）。

---

（以降、必要に応じて古いエントリを上に追記する形でも可）
